version: 0.1

phases:
  build:
    commands:
      - mvn -version
      - amazon-linux-extras install java-openjdk11
      - alternatives --config java < cat '1'
      - mvn install

artifacts:
  files:
    - webgoat-server/target/webgoat-server-8.0-SNAPSHOT.jar
  discard-paths: yes

